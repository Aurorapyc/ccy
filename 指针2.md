## 指针2

```c
#include<stdio.h>
int main(main)
{
    int num = 10;
    /*
    	1.通过&取到了num在内存中的地址
    	2.为了保存num的地址，使用一个变量p进行保存
    	3.那么这个保存地址的变量类型，我们成为指针变量类型
    	4.int* 是一种新的变量类型
    */
    int* p = &num;
    printf("变量num的值：%d, 指针变量p的值：%p\n",num,p);
    return 0;
}
```

